

This directory is for the scripts to prepare Data at the HDFS side
prepData.sh calling
	 prepPermData.sh*

# when you need to recreate the data. Need to run this to generate files in
# /tmp/PermTest, and copy to ~hdfsqa/hdfsRegressionData/PermTest
createLocalFiles.sh
createLocalFiles.sh


File list and directory list
perm-dirlist.txt
perm-filelist.txt

Data file - really small
smallFile14Byte
